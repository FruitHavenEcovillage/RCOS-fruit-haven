---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ProposalList from '../../../components/ProposalList.astro';
import { getCollection } from 'astro:content';

const all = await getCollection('proposals', (e) => e.data.status === 'passed');
const entries = all.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<BaseLayout
  title="Passed proposals"
  description="Proposals accepted by the Fruit Haven community."
>
  <article class="prose">
    <p class="eyebrow">Governance</p>
    <h1>Passed proposals</h1>
    <p>Decisions adopted by the community, sorted by decision date.</p>
  </article>
  <ProposalList
    entries={entries}
    status="passed"
    emptyText="No passed proposals yet."
  />
</BaseLayout>

<style>
  .eyebrow {
    text-transform: uppercase;
    font-size: 0.75rem;
    letter-spacing: 0.1em;
    color: var(--color-text-muted);
    margin: 0 0 0.25rem;
  }
</style>
